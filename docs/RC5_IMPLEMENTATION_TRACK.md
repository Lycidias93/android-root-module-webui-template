# RC5 implementation track

Status: active

This file tracks implementation work shared with SSH Drop Dispatcher RC5.

Planned generic core v0.3 primitives:

- typed collection/profile editing;
- schema-versioned bounded import/export;
- preview/apply transaction UX;
- rollback/result state rendering;
- secret-aware export policy metadata.

Consumer-specific SSH/target semantics remain outside the shared core.
